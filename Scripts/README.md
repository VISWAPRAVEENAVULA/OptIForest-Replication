# Scripts

This directory contains Python scripts used to run, evaluate, and support the OptIForest experiments.

**Files included:**
- `run_optiforest.py` — executes the main training and testing pipeline.
- `metrics_evaluation.py` — computes metrics such as AUC, Precision, Recall, and F1-score.

All scripts are modular and can be run independently or imported into notebooks.
